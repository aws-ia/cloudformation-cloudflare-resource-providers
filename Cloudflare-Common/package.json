{
  "name": "cloudflare-common",
  "version": "0.1.0",
  "description": "Common code for all Cloudflare resource types",
  "private": true,
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npx tsc",
    "watch": "npx tsc -w",
    "prepack": "npm run build",
    "test": "jest"
  },
  "dependencies": {
    "@amazon-web-services-cloudformation/cloudformation-cli-typescript-lib": "^1.0.3",
    "axios": "^0.27.2"
  },
  "optionalDependencies": {
    "aws-sdk": "^2.1354.0"
  },
  "devDependencies": {
    "@types/jest": "^28.1.2",
    "@types/node": "^14.0.0",
    "jest": "^28.1.1",
    "ts-jest": "^28.0.5",
    "typescript": "^4.1.2"
  }
}
